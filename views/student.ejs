<!DOCTYPE html>

<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="/css/bootstrap.css">
    <title>Student</title>
</head>
<body>
    <div class="container">
        <%if(message.length>0){ %>
            <div class = "alert alert-success">
                <%=message%>
            </div>
        <%}%>
        <h1 class="mb-4">Find Result</h1>
            <form action="/student" method="POST">
                <div class="form-group">
                    <label for="rollno">Rollno</label>
                    <input required value = "<%=student.rollno%>" type="text" name="rollno" id="rollno" class="form-control">
                </div>
                <div class="form-group">
                    <label for="name">Name</label>
                    <input required value = "<%=student.name%>" type="text" name="name" id="name" class="form-control">
                </div>
                <button type="submit" class="btn btn-primary mt-2">Search</button>
                <a href="/student" class="btn btn-secondary mt-2">Clear</a>
            </form>
    </div>
</body>
</html>